name: 'Python Security Scanner'
description: 'Scans your Python code for vulnerabilities.'
inputs:
  token:
    description: 'Github Token'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.token }}
